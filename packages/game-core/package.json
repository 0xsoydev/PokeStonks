{
  "name": "game-core",
  "version": "0.0.1",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit --strict --target es2022 --moduleResolution bundler --module esnext --skipLibCheck index.ts"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  },
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  }
}
