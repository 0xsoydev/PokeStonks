# Copy to contracts/.env (gitignored) and fill in. NEVER commit a real key.
PRIVATE_KEY=0x
# Optional: a separate address to act as the game-server voucher signer (defaults to the deployer).
# VOUCHER_SIGNER=0x
